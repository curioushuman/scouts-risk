package web_event

import (
"scouts-risk/web"
)

templ Events() {
@web.Base() {
{ children... }
}
}

templ EventDescription(title string, description string) {
<div class="px-4">
  <h1 class="pb-0 mb-0">
    <a class="text-black text-base block" href="/event">Events ></a> { title }
  </h1>
  <p>{ description }</p>
</div>
}